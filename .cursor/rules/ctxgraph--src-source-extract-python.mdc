---
description: "Python — src/source-extract/python.ts"
globs: src/source-extract/python.ts
alwaysApply: false
---

## When to Read
- editing or refactoring `python.ts`

## Overview
- `src/source-extract/python.ts` (56 lines · 2 top-level symbols) — Python symbol and import extraction.

## Graph
```mermaid
graph LR
  python[python]
```

## Signatures

```typescript
// ── src/source-extract/python.ts ──
/** Python symbol and import extraction. */
export function extractPythonSymbolLines(py: string): string[] { /* ~22 lines */ }
/** `import x` / `from pkg import` / relative `from ... import` (dependency hints). */
export function extractPythonImports(py: string): string[] { /* ~29 lines */ }

```

## Dependencies
- No dependencies detected

<!-- context-graph: generated from `.github/instructions/src/source-extract/python.instructions.md` -->
_Regenerate: `context-graph build --no-llm` · Do not hand-edit this rule._